---
type: dashboard
area: invest-swing
updated: 2026-07-05
cssclasses:
  - dashboard-v2
---

```dataviewjs
// ── Nav ──────────────────────────────────────────────────────────
const nav = dv.container.createEl("div", { cls: "app-topnav" });
const navItems = [["今日主控","Home"],["品牌","品牌中樞"],["投資","投資中樞"],["波段","波段中樞"],["生活","生活自我成長中樞"],["快速捕獲","快速捕獲中樞"],["雙週復盤","發文戰情"],["系統藍圖","系統藍圖"]];
for (const [label, target] of navItems) {
  const a = nav.createEl("a", { cls: "app-nav-item internal-link", text: label, href: "#" });
  a.setAttribute("data-href", target);
}

// ── 工具函式 ──────────────────────────────────────────────────────
const R = dv.container;
function el(parent, tag, opts = {}) {
  const { cls, text, html, attr = {} } = opts;
  const node = parent.createEl(tag, { cls, text });
  if (html) node.innerHTML = html;
  Object.entries(attr).forEach(([k, v]) => node.setAttribute(k, v));
  return node;
}
function openPage(name) {
  const f = app.vault.getAbstractFileByPath(name + '.md') || app.vault.getAbstractFileByPath(name);
  if (f) { app.workspace.getLeaf(false).openFile(f); return; }
  const base = name.split('/').pop();
  const byName = app.vault.getFiles().find(x => x.basename === base);
  if (byName) { app.workspace.getLeaf(false).openFile(byName); return; }
  app.workspace.openLinkText(name, '', false);
}
function openFile(path) {
  const f = app.vault.getAbstractFileByPath(path);
  if (f) app.workspace.getLeaf(false).openFile(f);
}

// ── 收合式 section 工廠 ───────────────────────────────────────────
function makeSection(parent, title, defaultOpen = false) {
  const card = parent.createEl('div', { cls: 'db-card', attr: { style: 'margin-bottom:12px;' } });
  const hdr  = card.createEl('div', { cls: 'db-card-header', attr: { style: 'cursor:pointer;user-select:none;' } });
  el(hdr, 'div', { cls: 'db-card-title', text: title });
  const chev = el(hdr, 'div', { attr: { style: 'font-size:13px;color:var(--db-text-muted);' }, text: defaultOpen ? '▾' : '▸' });
  const body = card.createEl('div', { attr: { style: `display:${defaultOpen ? 'block' : 'none'};padding-top:6px;` } });
  hdr.addEventListener('click', () => {
    const open = body.style.display !== 'none';
    body.style.display = open ? 'none' : 'block';
    chev.textContent   = open ? '▸' : '▾';
  });
  return body;
}

// ── 外框 ──────────────────────────────────────────────────────────
const wrap = R.createEl('div', { attr: { style: 'max-width:860px;margin:0 auto;padding:12px 0;' } });

// ── Header 卡 ─────────────────────────────────────────────────────
{
  const hc = wrap.createEl('div', { cls: 'db-card', attr: { style: 'margin-bottom:16px;padding:20px 24px;' } });
  el(hc, 'h1', { attr: { style: 'font-size:22px;font-weight:700;margin:0 0 6px;' }, text: '📊 財報頻道' });
  el(hc, 'div', { attr: { style: 'font-size:13px;color:var(--db-text-muted);' }, text: '對應 Discord #財報分析 頻道。在 Discord 下指令，結果自動同步到此頁。' });
  const linkRow = hc.createEl('div', { attr: { style: 'margin-top:10px;' } });
  const b = linkRow.createEl('button', { cls: 'db-btn db-btn-ghost db-btn-sm', text: '📌 個股總覽（持倉+估值+模型+研究一次看）' });
  b.addEventListener('click', () => openPage('03 Invest-Swing/個股總覽'));
}

// ════════════════════════════════════════════════════════════════
// 1. 指令說明（每個指令可獨立展開）
// ════════════════════════════════════════════════════════════════
const cmdBody = makeSection(wrap, '🛠️ 指令說明', true);

const CMDS = [
  {
    cmd:  '/財報 [代號]',
    desc: '全持倉估值快照，含成本損益、距停損距離、Claude 解讀',
    usage: [
      '/財報　→ 分析全部持倉（波段 + DCA + 觀察）',
      '/財報 2330 6139　→ 只分析指定代號',
    ],
    freq: '每週一次（週末複盤）＋季報出後',
    why:  'TWSE PE/EPS 每季才更新，天天跑看不出差異；週末複盤時搭配波段中樞使用最有價值。',
    tip:  null,
  },
  {
    cmd:  '/建模 [代號]',
    desc: '從 MOPS 抓近4季 EPS，計算 trailing PE、Graham Number、理論空間',
    usage: ['/建模 6139'],
    freq: '每季一次（季報出後 1-2 週內）＋考慮加減碼前',
    why:  'EPS 資料每季才有新值，模型不需要頻繁重建。要買賣前跑一次確認估值在合理區間。',
    tip:  null,
  },
  {
    cmd:  '/研究 [代號或關鍵字]',
    desc: '抓 Google News ＋ TWSE 重大訊息 → Claude 整理利多利空與短期影響',
    usage: [
      '/研究 6139　→ 亞翔相關新聞',
      '/研究 AI晶片　→ 產業關鍵字搜尋',
    ],
    freq: '有事件才跑（季報前後、大盤異動、考慮買賣時）',
    why:  '每天跑產出雜訊 > 訊號，事件驅動時效益最高。',
    tip:  null,
  },
  {
    cmd:  '/earnings-preview [代號]',
    desc: '財報前：Claude 根據近4季EPS告訴你本次必盯指標 ＋ 三種股價反應情境',
    usage: ['/earnings-preview 6139'],
    freq: '財報公布前 3–7 天跑一次',
    why:  '有「預期基準」才能在財報當天快速判斷是否超預期，不會慌亂。',
    tip:  null,
  },
  {
    cmd:  '/earnings-analysis [代號]',
    desc: '把財報相關文字丟進 Claude → 核心業績 / 各事業線 / 展望 / 風險訊號 / 投資結論',
    usage: [
      '短文字：/earnings-analysis 2330，Shift+Enter 換行後貼內容',
      '長文字：存成 .txt 拖進 DC 附件，指令只打 /earnings-analysis 2330',
      '來源：法說會投影片 PDF 複製文字、MOPS 重大訊息、外資報告片段',
    ],
    freq: '每季財報公布後 1 週內',
    why:  '管理層措辭、展望方向是純數字看不到的訊號，一季一次聚焦即可。',
    tip:  '📌 MOPS 查法說會：mops.twse.com.tw → 重大訊息與公告 → 法人說明會一覽表',
  },
];

CMDS.forEach(c => {
  const box  = cmdBody.createEl('div', { attr: { style: 'border:1px solid var(--db-border);border-radius:var(--db-radius-inner);margin-bottom:8px;overflow:hidden;' } });
  const bHdr = box.createEl('div', { attr: { style: 'display:flex;align-items:center;justify-content:space-between;padding:10px 14px;cursor:pointer;background:var(--db-bg-deep);user-select:none;' } });
  const bLeft = bHdr.createEl('div', { attr: { style: 'min-width:0;' } });
  el(bLeft, 'code', { attr: { style: 'font-size:13px;font-weight:700;color:var(--db-cream);display:block;' }, text: c.cmd });
  el(bLeft, 'span', { attr: { style: 'font-size:11px;color:var(--db-text-muted);' }, text: c.desc });
  const bChev = el(bHdr, 'span', { attr: { style: 'font-size:12px;color:var(--db-text-muted);flex-shrink:0;margin-left:10px;' }, text: '▸' });

  const bBody = box.createEl('div', { attr: { style: 'display:none;padding:12px 14px;font-size:12px;line-height:1.8;border-top:1px solid var(--db-border);' } });

  el(bBody, 'div', { attr: { style: 'font-size:10px;color:var(--db-text-muted);text-transform:uppercase;letter-spacing:.08em;margin-bottom:4px;' }, text: '使用方式' });
  c.usage.forEach(u => el(bBody, 'div', { attr: { style: 'color:var(--db-text-secondary);padding:1px 0;' }, text: '• ' + u }));

  el(bBody, 'div', { attr: { style: 'font-size:10px;color:var(--db-text-muted);text-transform:uppercase;letter-spacing:.08em;margin:10px 0 4px;' }, text: '最佳頻率' });
  el(bBody, 'div', { attr: { style: 'color:var(--db-cream);' }, text: '🕐 ' + c.freq });

  el(bBody, 'div', { attr: { style: 'font-size:10px;color:var(--db-text-muted);text-transform:uppercase;letter-spacing:.08em;margin:10px 0 4px;' }, text: '原因' });
  el(bBody, 'div', { attr: { style: 'color:var(--db-text-secondary);' }, text: c.why });

  if (c.tip) {
    el(bBody, 'div', { attr: { style: 'margin-top:10px;padding:8px 10px;background:var(--db-bg-card);border-radius:6px;color:var(--db-text-muted);font-size:11px;' }, text: c.tip });
  }

  bHdr.addEventListener('click', () => {
    const open = bBody.style.display !== 'none';
    bBody.style.display = open ? 'none' : 'block';
    bChev.textContent   = open ? '▸' : '▾';
  });
});

// ════════════════════════════════════════════════════════════════
// 2. 最新財報分析（點擊開啟 財報摘要.md）
// ════════════════════════════════════════════════════════════════
const sumBody = makeSection(wrap, '📈 最新財報分析');
const sumBtn = sumBody.createEl('div', {
  attr: { style: 'display:flex;align-items:center;gap:14px;padding:14px;background:var(--db-bg-deep);border-radius:var(--db-radius-inner);cursor:pointer;border:1px solid var(--db-border);margin-bottom:4px;' }
});
el(sumBtn, 'span', { attr: { style: 'font-size:28px;flex-shrink:0;' }, text: '📋' });
const sumInfo = sumBtn.createEl('div');
el(sumInfo, 'div', { attr: { style: 'font-size:14px;font-weight:600;color:var(--db-cream);' }, text: '財報摘要.md' });
el(sumInfo, 'div', { attr: { style: 'font-size:12px;color:var(--db-text-muted);' }, text: '由 /財報 指令生成，點擊開啟完整摘要' });
sumBtn.addEventListener('click', () => openPage('03 Invest-Swing/財報摘要'));

// ════════════════════════════════════════════════════════════════
// 3. 財務模型庫
// ════════════════════════════════════════════════════════════════
const modelBody = makeSection(wrap, '🏗️ 財務模型庫');
const modelFiles = dv.pages('"03 Invest-Swing/財務模型"').sort(p => p.file.mtime, 'desc').array();
if (modelFiles.length === 0) {
  el(modelBody, 'div', { attr: { style: 'color:var(--db-text-muted);font-size:12px;padding:8px 0;' }, text: '尚無建模記錄，用 /建模 [代號] 建立第一筆。' });
} else {
  modelFiles.forEach(p => {
    const row = modelBody.createEl('div', { attr: { style: 'display:flex;align-items:center;justify-content:space-between;padding:9px 6px;border-bottom:1px solid var(--db-border);cursor:pointer;' } });
    el(row, 'span', { attr: { style: 'font-size:13px;color:var(--db-purple);' }, text: p.file.name });
    const mt = new Date(p.file.mtime);
    el(row, 'span', { attr: { style: 'font-size:11px;color:var(--db-text-muted);flex-shrink:0;' }, text: `${mt.getMonth()+1}/${mt.getDate()} ${String(mt.getHours()).padStart(2,'0')}:${String(mt.getMinutes()).padStart(2,'0')}` });
    row.addEventListener('click', () => openFile(p.file.path));
  });
}

// ════════════════════════════════════════════════════════════════
// 4. 市場研究記錄
// ════════════════════════════════════════════════════════════════
const resBody = makeSection(wrap, '🔍 市場研究記錄');
const resFiles = dv.pages('"03 Invest-Swing/市場研究"').sort(p => p.file.mtime, 'desc').array().slice(0, 10);
if (resFiles.length === 0) {
  el(resBody, 'div', { attr: { style: 'color:var(--db-text-muted);font-size:12px;padding:8px 0;' }, text: '尚無研究記錄，用 /研究 [代號或關鍵字] 建立第一筆。' });
} else {
  resFiles.forEach(p => {
    const row = resBody.createEl('div', { attr: { style: 'display:flex;align-items:center;justify-content:space-between;padding:9px 6px;border-bottom:1px solid var(--db-border);cursor:pointer;' } });
    el(row, 'span', { attr: { style: 'font-size:13px;color:var(--db-teal);' }, text: p.file.name });
    const mt = new Date(p.file.mtime);
    el(row, 'span', { attr: { style: 'font-size:11px;color:var(--db-text-muted);flex-shrink:0;' }, text: `${mt.getMonth()+1}/${mt.getDate()} ${String(mt.getHours()).padStart(2,'0')}:${String(mt.getMinutes()).padStart(2,'0')}` });
    row.addEventListener('click', () => openFile(p.file.path));
  });
}
```
