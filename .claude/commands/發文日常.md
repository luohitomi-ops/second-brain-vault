---
description: 一次完成每天的發文例行：計畫表 + 待回填 + 今日文案建議
---

執行檸子的每日發文例行（一次到位，取代每天分開問的四件事）：

## 1. 這幾天計畫表
讀 `C:\tia\vault\01 Brand\社群數據\content_plan.md`，列出今天起 3 天內各帳號該發什麼（含撥撥每 3 天一支 Reels）。

⚠️ **路徑規則（2026-07-15 修正，防資料落差）**：`content_plan.md`／`history.md` 一律用這個 vault 真實路徑讀寫，**不要用 `C:\Users\USER\.claude\skills\social-post\` 底下那個路徑**——那是 symlink，Edit 工具會拒絕透過它寫入（不是權限問題，是工具內建安全機制，改 settings.json 也沒用）。用 skill 資料夾路徑讀取沒事，但這個指令步驟4要「寫回」戰績，一路用 vault 路徑最安全，不用每次現場才發現寫不進去。

⚠️ **跨表去重（2026-07-13 新增，修「只生得出植芮堂、其他不出」bug）**：content_plan.md 有多個排程子表——「第N輪個人帳號日曆」是**當前主排程（唯一真相來源）**，另有「霸王之業聲播」「植芮堂業配」等**專屬事件子表**會把同一則貼文也列一次（方便集中追蹤該事件）。判斷「今天/這幾天該發什麼」時，**以最新一輪「第N輪」為準**，專屬子表只拿來讀草稿/規則，**同一個 日期+平台+主題 出現在多張表時當成同一則、不要重複列，也不要因為兩表狀態寫法不同就卡住**。只有出現在專屬子表、但當前輪表完全沒有的項目（例如更早日期的霸王場次），才另外補列。

## 2. 待回填數據
**每次都要重新 Read `C:\tia\vault\01 Brand\社群數據\history.md` 最新內容再判斷，不能用對話記憶裡看過的舊版本回答**——同一頻道的對話可能延續很多天，中間資料早已更新，只憑記憶回答會把已回填的數據誤報成「還沒填」。

讀 `history.md`（同上 vault 路徑），列出「已發出 >48h 但戰績空白」的貼文。
- ⚠️ 撥撥每支 = IG ＋ TikTok 兩筆，TikTok 也要單獨列。
- ⚠️ **精確算小時，不要只看日期差幾天**：備註欄若有寫實際發文時間，用那個時間點算距今是否真的超過 48 小時，不能用「今天日期 - 貼文日期 ≥ 2 天」粗略估算——這樣在邊界時間（例如貼文剛發不到一天）會誤判成逾期。跟 `每日校準.md` 的判斷邏輯保持一致。

## 3. 靈感素材盤點 → F 公式題材建議
先讀 `07 Quick Capture & Hub/每日碎片化靈感隨記.md`，取出所有標記 `[發文素材]` 的條目（不論有無 ✅，✅ 只代表整理過，不代表已發文）。
接著結合 `C:\Users\USER\.claude\skills\social-post\references\formulas.md`（F1-F29，含 F15mini/F18/F19）+ `C:\Users\USER\.claude\skills\social-post\style_profile.md` 語氣 + [[每日熱門話題摘要]]，
針對每則素材判斷：
- 這個素材適合發到哪個帳號？
- 用哪個 F 公式切入最有爆款潛力？
- 一句切入角度是什麼？

輸出格式（每個素材一行）：
`素材摘要 → 【帳號 / F幾 / 切入角度】`

最後綜合 content_plan 今日排程，產出今日 1-2 則完整草稿（優先選靈感池裡最強的素材）。

## 4. 收尾提醒
- **戰績回填（遵守 48H standing rule，2026-07-13 確立）**：只提第 2 步篩出的「已發出 >48h 仍空白」清單；**未滿 48h 的貼文一律不提、不問、不列「在等」**——不要每天問「今天/昨天戰績要不要回填」。第 2 步清單是空的就說「目前無滿 48h 待回填」，不要硬找話題問。
- 若這週數據夠多，提醒我可以跑 `/爆款優化`；**距上次雙週復盤不到約 10 天就別建議跑**（新數據不夠，會被打槍）。
- **英文微動作：目前暫緩，不要主動問**（2026-07-14 確認：使用者最近忙、沒在執行；連續每天問=nagging）。這件事挪到低頻的 `/雙週復盤` 或 `/生活深聊` 才碰，`/發文日常` 不再每天問。等使用者主動說「恢復英文」再放回例行。

只做規劃與建議，不實際發佈（發佈要我打「確認」+ 電腦 Chrome）。

---
**元規則（2026-07-14）**：收尾提醒每一項，講之前先想「這是不是昨天/前幾天已經講過、已經有 standing rule 的？」——已確立的規則就照規則做，不要每天當新問題重問。要問使用者的，只有「今天真的需要他決定、且還沒決定過」的事。
