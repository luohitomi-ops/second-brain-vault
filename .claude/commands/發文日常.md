---
description: 每天的發文例行：看排程+行事曆建議（不強制主題）；使用者給主題+大綱時套F公式生成爆款+拆解邏輯
---

執行檸子的每日發文例行：

## 1. 這幾天計畫表（建議性質，2026-07-16 起不強制主題）
讀 `C:\tia\vault\01 Brand\社群數據\content_plan.md`，列出今天起 3 天內各帳號已經確定的排程（帳號+主題+時段）。

**14天排程平台（必排，2026-07-18 確立）**：FB個人／Gracie粉專／撥撥獺獺TikTok。

**彈性平台（不強制照日期排程）**：個人IG／撥撥獺獺IG／Threads系列——不用主動幫這幾個排題材；她給主題+大綱時一樣套F公式生成+附拆解邏輯，也可以手動要求「排進content_plan.md」或「回填數據」，單次處理即可，不用等進入每週固定排程。

⚠️ **不強制發文主題（standing rule）**：content_plan 沒有排定具體主題是正常狀態，不要自己腦補題材硬填進去，也不要因為「空著」就催她。可以做的是：讀 `00 Dashboard/today-calendar.json`，如果近幾天有工作/事件類行程，**給一句建議**（例如「明天有重機展，要不要拍工作照發 Gracie 粉專？」），但講清楚是建議、不是排定，她可以完全不理。

⚠️ **路徑規則（2026-07-15 修正，防資料落差）**：`content_plan.md`／`history.md` 一律用 vault 真實路徑（`C:\tia\vault\01 Brand\社群數據\`）讀寫，**不要用 `C:\Users\USER\.claude\skills\social-post\` 底下那個路徑**——那是 symlink，Edit 工具會拒絕透過它寫入（不是權限問題，是工具內建安全機制）。

⚠️ **跨表去重**：content_plan.md 有多個排程子表——「第N輪個人帳號日曆」是**當前主排程（唯一真相來源）**，另有「霸王之業聲播」「植芮堂業配」等**專屬事件子表**會把同一則貼文也列一次。判斷「這幾天該發什麼」時以最新一輪「第N輪」為準，同一個日期+平台+主題出現在多張表時當成同一則。

## 2. 待回填數據——不主動列（2026-07-16 起改週回填制）
`/週回填` 已經每週自動處理數據回填。**這裡不要再主動掃描 history.md 列 48h 待回填清單**。使用者主動問才查（手動輸入功能保留，格式不變：`M/DD 平台 標題：讚__ 留__ 分享__ 觀看__`，週回填抓不到的還是能這樣手動補）。

## 3. 靈感素材盤點（僅供參考，不主動生成）
讀 `07 Quick Capture & Hub/每日碎片化靈感隨記.md`，取出所有標記 `[發文素材]` 的條目（不論有無 ✅），列成候選清單給她看：

`素材摘要 → 建議帳號 / 建議 F 幾 / 一句切入角度`

**列完就停，不要自動挑一則生成完整草稿。** 生成草稿只在她明確給主題（或選了候選清單裡的一則、或說「就用這個」）之後才做，見下方步驟4。

## 4. 主題確定後 → 套 F 公式生成爆款（核心生成規則，任何時候使用者給主題+大綱都適用，不限定要跑 `/發文日常`）
使用者給「主題＋大綱」（可能只是一兩句話，不用寫得多完整）後：

1. 讀 `C:\Users\USER\.claude\skills\social-post\references\formulas.md`（F1-F29）+ `style_profile.md`（語氣），依主題性質挑最適合的 F 公式（工作照→F2、深度反思→Mode C系列、Reels→F18…）。
2. 生成草稿——**只用她給的主題和大綱裡的事實，不腦補沒說過的細節**（跟現有 R34 真實 voice 規則一致）。
3. **拆解邏輯一定要附上**（2026-07-16 新規則，不能只給文案）：
   - 用了哪個 F 公式、為什麼選這個（不是別的）
   - 逐段對照：這一段對應公式的哪個環節（hook/鋪陳/punch/CTA…）
   - 這個公式過去戰績大概什麼水準（有 history.md 數據可查的話簡短帶一句）
4. **標「微調安全區」一定要附上**（2026-07-17 新規則，跟拆解邏輯同時給，不能只給文案）：目的是讓她自己改字句時知道哪裡動了沒差、哪裡動了會破壞公式骨架，之後微調完不用再傳回來確認怕動到公式。格式：
   ```
   【可自由改】：具體是哪些字句/舉例/語氣細節可以她自己換掉，不影響公式生效
   【不要動】：對照 formulas.md 該公式「骨架」欄位，逐項列出結構性元件（例如開頭鉤子的位置/類型、CTA放的段落、幾段式結構的順序）——動了這些等於公式沒套上
   ```
   判斷依據＝ formulas.md 骨架欄位裡「必須存在的元件/順序」＝不要動；骨架沒規定的（字句選字、舉例內容、語氣細節）＝可自由改，不用重新分析。
5. 發佈前一樣要她打「確認」+ 電腦 Chrome，草稿階段不自動發。

## 5. 收尾提醒
- 若距上次 `/雙週復盤` 已經 14 天左右，提醒可以跑一次（每兩週數據大復盤是固定節奏，見 `雙週復盤.md`）。
- **英文微動作：目前暫緩，不要主動問**（使用者最近忙、沒在執行）。等使用者主動說「恢復英文」再放回例行。

只做規劃與建議，不實際發佈（發佈要我打「確認」+ 電腦 Chrome）。

---
**元規則（2026-07-14）**：收尾提醒每一項，講之前先想「這是不是昨天/前幾天已經講過、已經有 standing rule 的？」——已確立的規則就照規則做，不要每天當新問題重問。要問使用者的，只有「今天真的需要他決定、且還沒決定過」的事。
