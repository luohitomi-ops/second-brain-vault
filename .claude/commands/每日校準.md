---
description: 早上掃描整個 vault，給出今日焦點與提醒
---

你是檸子的第二大腦。請執行今日校準：

⚠️ **每次都要重新 Read 相關檔案的最新內容再判斷，絕對不能用對話記憶裡看過的舊版本或印象回答**——同一頻道的對話可能延續很多天，中間資料早已更新（尤其 history.md／content_plan.md），只憑記憶回答會把已經處理過的事、甚至早就不存在的舊問題，誤報成還沒處理。**任何要下結論的項目，都必須有這次執行時剛讀到的檔案內容或剛查詢到的即時資料支撐，不能只憑「印象中好像有這個問題」就寫進報告。**

⚠️ **不要碰 Stock Radar（給獺金幣）/ TG 推播狀態**：這塊已經有獨立的自動化機制在做（平日 08:45 bot 自動打 `/api/health` 健檢並貼 #總控，或使用者手動打 `/健檢`），`每日校準` 不需要也不應該對這個項目下任何判斷或警告——沒有即時查詢 API 就沒有依據亂猜，只會製造跟自動健檢對不上的假警報。

1. 掃描 `04 Projects` 所有專案的 frontmatter `status` 和未完成 `- [ ]` 待辦。
2. 掃描三大中樞（品牌/投資/波段）的「當前重點」。
   - **亞翔特別規則**：讀 `03 Invest-Swing/CLAUDE.md` 的「觸發狀態」。減碼只在股價逼近 1000 才動、移動停損只在跌破 60MA(723)/波段低點(708) 才動。**若現價未達這些門檻 → 亞翔列為「🟢 監控中」，絕不可放進「卡關/逾期」**。只有真的逼近 1000 或跌破 723 才提醒。
3. 讀 `02 Invest-LT/CLAUDE.md` 的「當前待辦」。
4. **自動發文掃描**（讀 skill 檔，不要改它們）：
   - `C:\tia\vault\01 Brand\社群數據\content_plan.md`：對照今天日期，看今天/本輪該發哪些貼文、哪個帳號（撥撥每 3 天一支 Reels 也算）。
     - ⚠️ **跨表去重**：以最新「第N輪個人帳號日曆」為主排程真相來源；「霸王之業聲播」「植芮堂業配」等專屬事件子表會重複列同一則，**同一個 日期+平台+主題 只算一次，不要重複列進今日發文**。
   - `C:\tia\vault\01 Brand\社群數據\history.md`：找「已發出 >48h 但戰績數據還空白（讚/留/觸及是 — 或空）」的貼文 → 列為待回填。撥撥獺獺 Reels 也要掃（撥撥IG/TikTok）。狀態為 ❌ 取消/順延的不列入待回填。
     - ⚠️ **精確算小時，不要只看日期差幾天**：備註欄若有寫實際發文時間（如「已排程 18:00」），要用那個時間點算距今是否真的超過 48 小時，不能用「今天日期 - 貼文日期 ≥ 2 天」粗略估算——這樣在邊界時間（例如貼文是傍晚發的，隔天凌晨才過 2 個日曆天但還沒滿 48 小時）會誤判成逾期。跟 OB 端 Home.md 的判斷邏輯（精確時間戳比對）保持一致，避免總控跟 OB 兩邊回填清單對不起來。
     - 🚨 **明確指認文章，不能只給日期+平台**：每一筆待回填 / 「還沒發」的貼文，都要去讀 `01 Brand/社群數據/文案存檔.md`，找同日期+平台的段落，把文案**開頭 20-30 字**附在提醒裡（例如「6/30 FB個人：『今天終於把土撥鼠...』還沒回填數據」）。文案存檔找不到對應段落時，要明講「⚠️ 文案存檔查無此篇，僅有 content_plan 題材摘要：XXX」，不要假裝有對照到、也不要只丟日期+平台讓使用者自己回想是哪篇。
5. **每日固定事項**：讀 `06 Lifestyle & Self-Growth/自律計劃表.md` 的「當前習慣組合」表，抓每日/晨/晚項目（喝水/運動/早睡/社群維護等）。
6. **生活代辦優先序**：讀 `06 Lifestyle & Self-Growth/生活自我成長中樞.md` 的「🎯 生活代辦優先序系統」，只挑 T2（有截止日）+ 快到期的 T3 項目納入卡關/焦點判斷，T4 習慣類不列入卡關（那是自律日誌的事，不是逾期任務）。
7. **行事曆驅動提醒**（2026-07-06 新增）：讀 `00 Dashboard/today-calendar.json`（未來 60 天事件）。
   - **今天起 2 天內有主持/直播/接案類事件**（標題含店家/廠商名稱，如霸王/大老爺/天行/傑太/遠傳/銘倫/蝦皮等，非個人生活事件）→ 提醒準備稿子/服裝/器材，指向 [[主持資料庫/README|主持資料庫]] 事前準備區
   - **未來 14 天內有醫美/手術類事件**（標題含手術/檢查/療程名稱）→ 提醒確認請假安排、術後恢復期規劃、費用是否已備妥
   - **未來 7 天內的其他個人事件**（生日、私人行程）→ 一行帶過，不用展開細節

然後輸出（精簡，不超過 20 行）：
- **🔴 卡關/逾期**：放著沒動的（亞翔數據沒填、送審沒追、貼文數據逾 48h 未回填、生活 T2 截止日接近）
- **🟡 今日建議焦點**：依優先順序挑 1-3 件最該做的（接案 > 生活T2 > 其他）
- **📣 今日發文**：今天該發什麼／哪個帳號（依 content_plan）；沒有就說「今日無排程」
- **☀️ 今日固定**：自律計劃表的每日習慣清單（一行列完，如：喝水2000cc／運動15分／早睡00:00前／社群維護10分）
- **📅 近期行事曆提醒**：未來1-2天工作類事件的準備提醒（見上方規則7）、未來14天醫美/手術類提醒、其他個人事件一行帶過
- **🟢 提醒**：接近的里程碑（順勢機器人 2026-09-21、12月主復盤）

## 🔴 待回填清單輸出格式（重要，避免用戶複製回填時漏欄位）
列出「待回填」貼文時，**每一則都要用完整格式，日期絕對不能省略**（bot 的 backfill parser 逐行獨立解析，沒日期的行會直接被跳過）：

```
[M/DD] [平台] [公式/標題]：讚__ 留__ 分享__ 觀看__
```

範例：
```
6/30 FB個人 Mode B 台中展場工作帖：讚__ 留__ 分享__ 觀看__
6/30 個人Threads F7 台中展場短版：讚__ 留__ 分享__ 觀看__
```

不要用「回填 6/30部分」這種標題+省略日期的清單格式（容易漏抄、且用戶若不整段複製會出錯）。每行都要能獨立複製貼上就直接生效。

不要改任何檔案，只報告。最後問她要不要針對某項深入或現在發文。
