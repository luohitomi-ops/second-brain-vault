---
description: 每週跑一次，用瀏覽器自動讀取FB個人/Gracie粉專/撥撥獺獺TikTok的貼文數據，自動寫進history.md，取代每天手動回填；個人IG/撥撥獺獺IG/Threads系列改為使用者主動要求才處理
argument-hint: 需要電腦Chrome + mcp__Claude_in_Chrome 可用
---

你是檸子的數據回填助手。目標：**取代每天手動報數字**，一週跑一次這個指令，把這週所有該回填的貼文數據自動讀出來寫進 `history.md`，不用她自己一則一則對照打字。

## 前提
- `mcp__Claude_in_Chrome__*` 必須可用（否則停止，告知使用者要開 Chrome MCP 才能跑）
- 使用者已登入 Facebook（FB個人 zhiningwei0526、Gracie粉專 graciening 共用登入）、Instagram（yuzukiluo 個人帳 + bobotata_ai 撥撥帳，若用同一個瀏覽器需注意帳號切換）、TikTok（@bobotataandcat）
- 這個指令處理**三個14天排程平台**：FB個人／Gracie粉專／撥撥獺獺TikTok（2026-07-18 確立，取代舊版四平台清單）。**個人IG／撥撥獺獺IG／Threads系列改成彈性平台，不列入每週自動掃描範圍**——這幾個平台的數據要回填，改成使用者主動說「回填 [平台] 數據」才處理，單次抓取即可；COS線目前停權/低頻，暫不處理
- **撥撥獺獺（IG Reels + TikTok）跟一般貼文抓法不同**（Reels 數據顯示位置、TikTok 是完全不同網站要另外登入），第一次跑大概率命中率會比 FB/IG 一般貼文低，這是預期中的事，不用勉強

## 步驟

1. **列出待回填清單**：讀 `C:\tia\vault\01 Brand\社群數據\history.md`，篩出「日期已過 48h」且「讚/觀看是 — 或空」的貼文，**只留平台是 FB個人／Gracie粉專／撥撥獺獺TikTok 的**（2026-07-18 起個人IG／撥撥獺獺IG 不再列入這份自動清單，改成使用者主動要求才處理）（跟 `/每日校準` 同一套 48h 精算邏輯，不要只看日曆天數差）。
2. **對照文案存檔取錨點文字**：讀 `C:\tia\vault\01 Brand\社群數據\文案存檔.md`，對每則待回填貼文找同日期+平台的段落，取文案前 15-20 字當比對錨點（畫面上找不到一模一樣的全文，抓開頭關鍵字片段即可）。撥撥系列沒有文字文案可比對，改用 history.md 備註欄的題材描述（例如「撥撥打拳」「土撥鼠×迷因貓跳舞」）當關鍵字。文案存檔查無對應段落的一般貼文，同樣改用備註欄題材描述，效果會差一點，先試試看。
> ⚠️ **2026-07-17 第一次真實測試發現**：FB 個人頁的 DOM 結構已經跟 `learn_style.md` 記錄的不一樣，`[role="article"]` + `data-ad-preview="message"` 選擇器現在抓不到東西（詳見 `learn_style.md` 對應段落的警告）；`get_page_text` 抓出來的數字/時間戳順序也常亂掉（跟畫面實際順序對不上）。**目前驗證有效的方法是：`navigate` 到頁面後用 `computer(action="screenshot")` 直接視覺讀取**——貼文正文＋讚/留言/分享數字都會清楚顯示在畫面上，用截圖比對錨點文字找到對應貼文，再直接讀畫面上的數字，比硬解析 DOM 可靠。JS 選擇器可以先試，但抓到空陣列或數字對不上時**直接改用截圖視覺讀取，不要卡在修 DOM 選擇器上**（FB 版型常變，選擇器過時是預期中的事）。

3. **按平台分組，一個平台只開一次頁面**（2026-07-18 起只跑這三個，個人IG／撥撥獺獺IG 見下方「彈性平台」章節）：
   - FB個人 → `navigate` 到 `facebook.com/zhiningwei0526`（或使用者個人頁網址）
   - Gracie粉專 → `navigate` 到 Gracie 粉專頁
   - 撥撥TikTok → `navigate` 到 `tiktok.com/@bobotataandcat`
   - 每個平台頁面**邊捲邊抓**（feed 會虛擬化，捲太快前面內容會從 DOM 卸載，抓法照 `C:\Users\USER\.claude\skills\social-post\references\learn_style.md` 「抓貼文」那節的技巧：優先讀頁面 DOM/JS，抓不到 fallback 用 `get_page_text`）
4. **比對貼文**：用步驟2拿到的錨點文字，在抓到的 feed 內容裡找對應貼文區塊（同時用日期輔助比對，避免文字相似但抓錯篇）。
5. **讀取互動數字**：
   - FB/Gracie粉專：讚數/留言數/分享數
   - 撥撥TikTok：讚數(♥)/留言數/分享數/觀看數
   - ⚠️ **不確定就別亂填**：數字顯示常常縮寫（例：1.2萬、1.2K）、或介面改版導致抓不到——**抓到就寫實際數字，抓不清楚或找不到對應貼文的，這則跳過、留在待回填清單，不要用猜的湊數字**。寧可少填幾則，不要填錯。
6. **寫回 history.md**：對每則成功比對+讀到數字的貼文，直接用 vault 真實路徑 `C:\tia\vault\01 Brand\社群數據\history.md` 更新該行（讚/留言/分享/觀看，格式照現有表格），不要用 skill 資料夾的 symlink 路徑（見 [[social-post更新規則]]）。
7. **content_plan.md 同步**：對應的排程行如果狀態還沒標 ✅已發，順手標一下（同樣走 vault 真實路徑）。
8. **記錄執行時間**：把這次執行的日期時間寫進 `C:\tia\vault\00 Dashboard\週回填最後執行.json`（格式 `{"lastRun":"YYYY-MM-DD HH:MM"}`，不存在就新建），讓 `/每日校準` 能判斷「多久沒跑了」該不該提醒。

## 收尾回報
分平台列出（FB個人/Gracie粉專 一組，撥撥TikTok 另一組，因為預期命中率不同）：
- ✅ 成功自動填入幾則（哪些貼文、抓到什麼數字）
- ⚠️ 抓不到/比對不到的幾則（列出來，這些還是需要使用者自己講數字或晚點重跑）
- 這次跑完後 history.md 還剩幾則待回填（14天排程平台範圍內）

## 彈性平台（個人IG／撥撥獺獺IG／Threads系列）數據回填（2026-07-18新增）
這幾個平台不列入上面的每週自動掃描，但使用者可以隨時說「回填 [平台] 數據」或「幫我抓一下 [平台] 這篇的數據」，單次觸發時：
1. 只針對使用者指定的平台/貼文執行上面步驟2-6同一套流程（錨點比對＋截圖視覺讀取＋寫回history.md）
2. 不用檢查48h門檻（使用者主動要求時通常已經知道該篇夠久了，除非明顯太新才提醒她可能還沒穩定）
3. 完成後一樣回報抓到什麼數字、寫進了 history.md 哪一行

## 原則
- 這是取代「每天回填」的關鍵功能，**目標是讓使用者一週只需要看一次回報，不用每天想著要不要回填**
- 抓不到數字不是這個指令的失敗，是誠實回報限制——平台頁面結構常變，選擇器失效是預期會發生的事，不要硬湊
- 第一次跑完，如果抓取失敗率偏高，回報使用者「這次自動化命中率大概 X%，可能是XX原因，要不要調整」，讓她知道這個功能還在磨合，不要默默每週都抓不到卻沒人發現
- 手動輸入的回填方式（Discord 打數字、Home.md 表單）**依然保留**，週回填抓不到或漏掉的，使用者還是可以照原本方式手動補
